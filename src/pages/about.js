import * as React from "react";
import Navigation from "../components/navigation"

const AboutPage = (
  content,
) => {
  return (
  <div>
    {content.test}
    <Navigation />
  </div>
  );
};

export default AboutPage;